----------------------------------------------
Consider a list (list = []). You can perform the following commands:

insert i e: Insert integer  at position .
print: Print the list.
remove e: Delete the first occurrence of integer .
append e: Insert integer  at the end of the list.
sort: Sort the list.
pop: Pop the last element from the list.
reverse: Reverse the list.
Initialize your list and read in the value of  followed by  lines of commands where each command will be of the  types listed above. 
Iterate through each command in order and perform the corresponding operation on your list.
----------------------------------------------


if __name__ == '__main__':
    N = int(input())
    list=[]
    for i in range(0,N):
        splitted_text=input().split()
        if splitted_text[0]=="insert":
            list.insert(int(splitted_text[1]),int(splitted_text[2]))
        elif splitted_text[0]=="append":
            list.append(int(splitted_text[1]))
        elif splitted_text[0]=="remove":
            list.remove(int(splitted_text[1]))
        elif splitted_text[0] == "pop":
            list.pop()
        elif splitted_text[0] == "sort":
            list.sort()
        elif splitted_text[0]=="reverse":
            list.reverse()
        elif splitted_text[0] == "print":
            print(list)



if __name__ == '__main__':
    N = int(input())
    my_list = []
    operations = {
        "insert": lambda x: my_list.insert(int(x[0]), int(x[1])),
        "append": lambda x: my_list.append(int(x[0])),
        "remove": lambda x: my_list.remove(int(x[0])),
        "pop": lambda x: my_list.pop(),
        "sort": lambda x: my_list.sort(),
        "reverse": lambda x: my_list.reverse(),
        "print": lambda x: print(my_list)
    }
    for _ in range(N):
        operation, *args = input().split()
        operations[operation](args)
